<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> <mapper namespace="com.etop.management.dao.BusinessServiceDao">            <select id="getBusinessService" parameterType="Map" resultType="com.etop.management.bean.Etopservice">			SELECT				*,category as categoryName			FROM				etop_service t1			INNER JOIN etop_service_business t2 ON t1.service_id = t2.service_id			INNER JOIN etop_service_quotation t3 ON t3.quotation_id = t2.quotation_id			WHERE			t1.service_type='SWFW'			AND			t1.club_id=#{clubId}			<if test="serviceNo != null and serviceNo != ''">			AND			   t1.service_no = #{serviceNo}			</if>			<if test="subject != null and subject != ''">			AND 			   t2.subject like '%${subject}%'			</if>			<if test="companyName != null and companyName != ''">			AND 			   t1.company_name like '%${companyName}%'			</if>			<if test="applyTime != null and applyTime != ''" >			AND				t1.apply_time >= #{applyTime}       		    </if> 		    <if test="completeTime != null and completeTime != ''" >			AND				 t1.complete_time &lt;= #{completeTime}       		    </if> 		    <if test="serviceStatus != null and serviceStatus != ''">			AND 			   t1.service_status = #{serviceStatus}			</if>		    <if test="categoryName != null and categoryName != ''">			AND 			   t3.category like '%${categoryName}%'			</if>      </select>             <select id="getBusinessServiceInfoById" resultType="com.etop.management.bean.Etopservice" parameterType="Map">			SELECT				*			FROM				etop_service t1			INNER JOIN etop_service_business t2			ON				t1.service_id = t2.service_id			WHERE			    t1.service_type='SWFW'			AND			   t1.service_id = #{serviceId}        </select>        		<!-- 商务服务维护-->        <select id="makeBusinessService" resultType="com.etop.website.bean.ServiceQuotation" parameterType="Map">				SELECT					*				FROM				    etop_service_quotation				WHERE type='service'   				<if test="parkId != null and parkId != ''">				AND				   park_id =#{parkId}				</if>				<if test="quotationName != null and quotationName != ''">				AND 				   quotation_name like '%${quotationName}%'				</if>				<if test="category != null and category != ''">				AND				   category like '%${category}%'				GROUP BY                    category				</if> 				        </select>                <select id="makeBusinessServiceGroup" resultType="com.etop.website.bean.ServiceQuotation" parameterType="Map">				SELECT					*				FROM				    etop_service_quotation				WHERE type='service'   				<if test="parkId != null and parkId != ''">				AND				   park_id =#{parkId}				</if>				<if test="quotationName != null and quotationName != ''">				AND 				   quotation_name like '%${quotationName}%'				</if>				<if test="category != null and category != ''">				AND				   category like '%${category}%'				</if> 				GROUP BY                    category        </select>        <select id="dispatchList" resultType="com.etop.management.bean.ServiceDispatch" parameterType="Map">				SELECT					*				FROM					etop_service_dispatch				WHERE service_id=#{serviceId}				        </select>           <insert id="addBusinessService"  parameterType="com.etop.website.bean.ServiceQuotation"  >		  INSERT INTO 		    etop_service_quotation (		         quotation_id,category,quotation_name,price,units,description,remark,type,create_time,park_id,activated		       )			VALUES( 				 #{quotationId},#{category},#{quotationName}, #{price},				 #{units},#{description},#{remark},#{type},#{createTime},#{parkId},#{activated}			   )      </insert></mapper>